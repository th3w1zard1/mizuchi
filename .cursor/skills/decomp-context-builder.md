# Skill: decomp-context-builder

Purpose: assemble compile-ready context for a prompt folder.

## Output checklist
- `prompts/<fn>/settings.yaml` populated with required keys
- type dependencies enumerated
- call graph essentials captured
- context gaps explicitly listed
