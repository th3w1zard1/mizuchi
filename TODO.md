- Mudar o Compiler para substituir todos os comentários com espaços, tanto /\* \*/ como //

# TODOs

O `claude-cache.json` não funciona mais, porque o path do arquivo de contexto muda a cada nova chamada.

##################

Talvez recriar os plugins a cada novo pipeline.
memory/per-prompt-plugin-recreation.md
